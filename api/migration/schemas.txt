FF:
table: models
(id, name, url, picture, phone, notes, display_order, extra_data)

extra_data: {"color":"#ABCDEF", "additionalPictures": ["http://..", ...]}



vipModels:
id integer primary key autoincrement,
	name text,
	category text,
	tags,
	notes text,
	private_notes,
	images text,
	hash text,
	display_order number, 
	instagram text, 
	phone text
)
